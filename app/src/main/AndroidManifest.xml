<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.best_of_best">

    <uses-permission android:name="android.permission.CAMERA"/> <!-- cranberryai -->
    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Best_of_best"
        tools:targetApi="31">


        <activity
            android:name=".login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".login_file.login_membership"/>
        <activity android:name=".login_file.login_new_pw"/>
        <activity android:name=".login_file.login_pwsearch"/>
        <activity android:name=".fragment.Fragment_mlkit_poseDetect"/>
        <activity android:name=".home"/>
        <activity android:name=".login_file.login_id"/>
        <activity android:name=".MainActivity"/>

        <activity android:name=".fragment.pop" android:theme="@android:style/Theme.Dialog" />
<!--        <activity android:name=".Counting"/>-->


    </application>

</manifest>